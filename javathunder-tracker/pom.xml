<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.oatelauser</groupId>
    <artifactId>javathunder</artifactId>
    <version>0.7.0</version>
  </parent>

  <artifactId>javathunder-tracker</artifactId>
  <name>JavaThunder :: Tracker</name>
  <description>Production tracker: HTTP announce (BEP 3/23), UDP announce (BEP 15), scrape (BEP 48), whitelist, stats/metrics endpoints; runnable jar.</description>

  <dependencies>
    <dependency>
      <groupId>io.github.oatelauser</groupId>
      <artifactId>javathunder-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
      <!-- 公共 API 守门员：任何破坏性 API 变更令构建失败（参数对齐 javathunder-api）。
           tracker 首发于 v0.3.0；基线随每版发布上移（当前 v0.6.0）。
           oldVersion 用 file 指向本地仓库基线 jar 而非 dependency 坐标：当前项目版本
           与基线同版本时，dependency 会被 reactor 解析成本模块刚构建的 jar，
           old==new 恒零差异，护栏失效（file 方式始终取仓库里安装的基线）。 -->
      <plugin>
        <groupId>com.github.siom79.japicmp</groupId>
        <artifactId>japicmp-maven-plugin</artifactId>
        <version>0.23.1</version>
        <configuration>
          <oldVersion>
            <file>
              <path>${settings.localRepository}/io/github/oatelauser/javathunder-tracker/0.6.0/javathunder-tracker-0.6.0.jar</path>
            </file>
          </oldVersion>
          <oldClassPathDependencies>
            <dependency>
              <groupId>io.github.oatelauser</groupId>
              <artifactId>javathunder-core</artifactId>
              <version>0.6.0</version>
            </dependency>
          </oldClassPathDependencies>
          <newClassPathDependencies>
            <dependency>
              <groupId>io.github.oatelauser</groupId>
              <artifactId>javathunder-core</artifactId>
              <version>${project.version}</version>
            </dependency>
          </newClassPathDependencies>
          <newVersion>
            <file>
              <path>${project.build.directory}/${project.build.finalName}.jar</path>
            </file>
          </newVersion>
          <parameter>
            <onlyModified>true</onlyModified>
            <breakBuildOnBinaryIncompatibleModifications>true</breakBuildOnBinaryIncompatibleModifications>
            <includes>
              <include>io.github.oatelauser.thunder.tracker</include>
            </includes>
          </parameter>
          <skip>${skipJapicmp}</skip>
        </configuration>
        <executions>
          <execution>
            <phase>verify</phase>
            <goals>
              <goal>cmp</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <!-- 附加 *-with-dependencies.jar：可执行（Main-Class=TrackerMain），主产物保持瘦 jar。
           配置继承根 pom，仅追加 Main-Class 清单。 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <configuration>
          <transformers>
            <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
              <mainClass>io.github.oatelauser.thunder.tracker.TrackerMain</mainClass>
            </transformer>
            <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
          </transformers>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
